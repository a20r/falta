version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
  settings:
    revive:
      rules:
        - name: exported
        - name: package-comments
          disabled: true

formatters:
  enable:
    - gofmt
